Developer guide
===============

TODO write up the PR guidance (which is a minimal git flow workflow), info on running the CI locally (python -m pytest -v tests), info on building the docs locally (sphinx-build docs _build)